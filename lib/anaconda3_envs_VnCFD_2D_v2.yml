name: VnCFD_Py3.7
dependencies:
  - python=3.7
  - numpy=1.17.*
  - matplotlib=3.1.*

# Creating an environment: conda env create -f environment.yml
# Updateing an environment: conda env update --prefix ./env --file environment.yml  --prune
